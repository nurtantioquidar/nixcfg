{
  # Your Git user configuration
  # Copy this file to secrets.nix and fill in your actual values
  userName = "Your Name";
  userEmail = "your.email@example.com";

  # SSH signing key (if you use 1Password for Git commit signing)
  # Get this from: 1Password -> SSH Key -> Copy Public Key
  # Or leave as empty string if you don't use commit signing
  sshSigningKey = "";
}
