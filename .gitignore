# Local development state
/.direnv/
/result
/.omx/
/.claude/settings.local.json
.DS_Store

# Local environment files
.env*
!.envrc

# Personal secrets (git user info, SSH keys, etc.)
secrets.nix
git-secrets.nix
